async function main(): Promise<void> {
  // Placeholder test runner so `npm test` remains functional until real tests are added.
  console.log('No automated tests are configured yet.');
}

main().catch(error => {
  console.error(error);
  process.exit(1);
});
